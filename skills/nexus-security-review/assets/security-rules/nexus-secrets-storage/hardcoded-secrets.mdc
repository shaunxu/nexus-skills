---
description: 检测 Nexus 应用源代码中的硬编码密钥、凭据与 API Key
globs:
alwaysApply: false
---

背景

- 若源码暴露（公开仓库、安装包、Marketplace 审核、源码泄露），其中的硬编码凭据会导致未授权访问。Nexus 应用应使用 KVS `secret: true` 存储敏感值，或使用平台提供的令牌注入机制（remote `auth.userToken`/`appToken`）。
- 相关 CWE：CWE-798（使用硬编码凭据）、CWE-259（使用硬编码密码）。
- 注意：Nexus 安装包会被上传到企业环境，应用代码对企业管理员并非完全不可见，不应将任何真实密钥打包进客户端/后端代码。

范围与信号

- 需要检测的密钥：
  - Basic Auth 头：`Basic [base64]`
  - Bearer Token：`Bearer [token]`
  - API Key：`api_key`、`apiKey`、`api-key`、`x-api-key` 等带字面量值
  - 云厂商凭据：`AKIA...`（AWS）、`LTAI...`（阿里云）、`AKID...`（腾讯云）等
  - 私钥：`-----BEGIN PRIVATE KEY-----`、`-----BEGIN RSA PRIVATE KEY-----`
  - OAuth 密钥：`client_secret` 带字面量值
  - 含密码的数据库连接串
  - Webhook 签名密钥
  - PingCode / 第三方 access token、refresh token
- 位置：源文件、配置文件、Custom UI 打包产物（不要将密钥放入 `web/main/`）。

检测模式

```bash
# Basic Auth 检测
grep -rlE '"Basic [A-Za-z0-9+/]*={1,2}"' --exclude-dir=node_modules --exclude-dir=dist .
grep -rlE "'Basic [A-Za-z0-9+/]*={1,2}'" --exclude-dir=node_modules --exclude-dir=dist .

# Bearer Token（JWT 形态）
grep -rE 'Bearer [A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+' .

# API Key（常见模式）
grep -rE '(api[_-]?key|apiKey)\s*[:=]\s*["'"'"'][A-Za-z0-9]{20,}["'"'"']' .

# AWS Access Key
grep -rE 'AKIA[0-9A-Z]{16}' .
```

脆弱模式

```typescript
// 脆弱 - 硬编码 Basic Auth
const headers = { Authorization: 'Basic dXNlcm5hbWU6cGFzc3dvcmQ=' };

// 脆弱 - 硬编码 API Key
const API_KEY = 'sk-1234567890abcdef1234567890abcdef';
await fetch.request(url, { headers: { 'X-API-Key': API_KEY } });

// 脆弱 - 配置中硬编码
const config = { database: { password: 'super_secret_password' } };

// 脆弱 - 硬编码 OAuth/Webhook 密钥
const webhookSecret = 'whsec_abc123-client-secret-xyz789';

// 脆弱 - 以普通 KVS 存储密钥（未加密）
await kvs.set('external-api-key', API_KEY); // 缺少 { secret: true }
```

安全模式

```typescript
import { kvs } from '@pc-nexus/core';

// 安全 - 从 KVS secret 中读取密钥（加密存储，读取时自动解密）
async function getApiKey() {
  return await kvs.get<string>('external-api-key', { secret: true });
}

// 安全 - 通过应用安装/配置流程写入 secret
// （例如在 lifecycle install 事件或管理员配置页中设置，不在源码中写入）
await kvs.set('external-api-key', adminProvidedKey, { secret: true });

// 安全 - 使用 remote auth 由平台注入令牌，避免手动管理
// manifest.yaml:
// remotes:
//   - key: internal-service
//     baseUrl: https://api.example.com
//     auth:
//       userToken: true
//       appToken: false
// 后端：
remote.invoke('internal-service', { path: '/me', method: 'GET' });
// 平台自动附加 Authorization: Bearer <NIT> 与 x-nexus-user-token

// 安全 - 运行时从安全存储中读取凭据，不在源码中硬编码
async function initializeClient() {
  const credentials = await kvs.get<string>('service-credentials', { secret: true });
  return new ServiceClient(JSON.parse(credentials));
}
```

检测清单

- [ ] 搜索 Base64 编码的 Basic Auth 头并解码确认。
- [ ] 查找 Bearer Token 模式（JWT 形态或不透明令牌）。
- [ ] 查找带字面量字符串的 API Key 变量赋值。
- [ ] 查找云厂商 Access Key 模式。
- [ ] 搜索私钥文件内容（`-----BEGIN ... PRIVATE KEY-----`）。
- [ ] 检查配置对象是否包含带值的 password/secret/client_secret 字段。
- [ ] 搜索 Custom UI 目录（`web/`）中是否存在密钥（前端不应持有任何应用密钥）。
- [ ] 检查 `kvs.set` 是否对敏感键使用 `{ secret: true }`。
- [ ] 排除测试夹具和 mock 数据，但需验证它们不包含真实凭据。

误报处理

```typescript
// 误报 - 示例/文档中的占位符
const API_KEY = 'YOUR_API_KEY_HERE';
const token = 'example-token-replace-me';

// 误报 - 测试夹具
const mockAuth = 'Basic dGVzdDp0ZXN0'; // test:test

// 误报 - 仅类型定义
interface Config { apiKey: string; }

// 真实漏报 - 真实凭据
const auth = 'Basic cHJvZHVjdGlvbjpyZWFsX3NlY3JldA=='; // 解码后是 production:real_secret
```

Semgrep 规则示例

```yaml
rules:
  - id: nexus-hardcoded-basic-auth
    pattern-regex: "Basic [A-Za-z0-9+/]{10,}={0,2}"
    message: "潜在硬编码 Basic Auth 凭据"
    severity: ERROR

  - id: nexus-hardcoded-api-key
    patterns:
      - pattern: $VAR = "..."
      - metavariable-regex:
          metavariable: $VAR
          regex: (api[_-]?key|apiKey|API_KEY|secret|SECRET|token|TOKEN)
      - pattern-not: $VAR = "YOUR_API_KEY"
      - pattern-not: $VAR = ""
    message: "潜在硬编码 API 密钥/令牌"
    severity: WARNING

  - id: nexus-kvs-secret-without-flag
    patterns:
      - pattern: kvs.set($KEY, $VALUE)
      - pattern-not: kvs.set($KEY, $VALUE, { secret: true, ... })
    message: "kvs.set 未使用 secret:true —— 若存储敏感数据请加密"
    severity: WARNING
```

PoC / 测试线索

- 对 Base64 Basic Auth 头解码确认是否为真实凭据。
- 检查硬编码 Key 是否可用（对外部服务进行测试）。
- 移除硬编码密钥后验证应用是否仍可工作（判断其是否实际被使用）。

修复建议（咨询）

- 立即从源码中移除所有硬编码密钥并轮换已暴露凭据。
- 运行时密钥使用 `kvs.set(key, value, { secret: true })` / `kvs.get(key, { secret: true })`。
- Remote 场景使用 `auth.userToken` / `auth.appToken` 让平台自动管理令牌。
- 在 CI/CD 中加入密钥扫描（gitleaks、trufflehog、GitHub Secret Scanning）。
- 不要将密钥放入 Custom UI 前端代码；前端 Custom UI iframe 可被用户调试。
- 对 webhook 签名等密钥，安装时由管理员通过配置页输入并存入 KVS secret。

报告指南

- 为每个硬编码密钥提供文件路径与行号。
- 指明密钥类型（Basic Auth、API Key、JWT、Webhook secret 等）。
- 区分真实凭据与占位符/示例值。
- 评估暴露风险（公开仓库、Marketplace 应用、企业内分发）。
- 映射到 CWE-798；建议立即轮换。
