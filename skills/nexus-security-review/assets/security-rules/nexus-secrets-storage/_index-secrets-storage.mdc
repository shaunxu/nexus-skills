---
description: Nexus 密钥与存储规则索引。对 nexus-secrets-storage/** 自动挂载
globs:
  - nexus-secrets-storage/**
alwaysApply: false
---

- 范围：密钥管理、凭据存储、敏感数据处理、KVS/CES/NOS 使用。
- 优先级：硬编码密钥与凭据管理不当是常见发现。

Nexus 密钥/存储关键风险

- 硬编码凭据：Basic Auth 头、API Key、Token、私钥、Webhook 签名密钥嵌入源码/配置。
- 密钥存储不当：对敏感值使用 `kvs.set(key, value)`（未加密）而非 `kvs.set(key, value, { secret: true })`。
- PAT/API/OAuth Token 处理：手动管理令牌而非使用 `remote.auth.userToken` / `appToken` 由平台注入。
- 日志中的密钥：在 console/logger 中输出 `Authorization`、`x-nexus-*`、NIT、PingCode access token、Webhook 签名。
- `environment.variables` 误用：该字段在构建/部署时注入，且**不支持加密**，仅适合非敏感配置；绝不能用于存放密钥、令牌、密码。
- NOS 对象未加密或公开：预签名 URL 生成前未做授权，导致任意用户可下载他人文件。
- CES 中存放敏感字段：字段级加密需应用自行处理；敏感数据应使用 KVS secret 或独立加密。

子规则

- 硬编码密钥 → `@nexus-secrets-storage/hardcoded-secrets.mdc`
<!-- - 存储 API 安全 → `@nexus-secrets-storage/storage-api-security.mdc`
- 令牌与凭据处理 → `@nexus-secrets-storage/token-handling.mdc`
- 日志中的密钥 → `@nexus-secrets-storage/secrets-in-logs.mdc` -->

检测启发式

- grep `Basic [A-Za-z0-9+/]*={1,2}` 模式（排除 node_modules）。
- 搜索 `kvs.set(` 且键名包含 `secret`、`key`、`token`、`password`、`credential`，但未传 `{secret:true}`。
- 搜索 `Authorization: Bearer ...`、`x-api-key` 等带有字面量值的请求头。
- 搜索 `console.log/info/error` 中包含敏感变量名（token、secret、password、authorization、cookie、nit、x-nexus-*）。
- 搜索 NOS `createDownloadUrl/delete/getMetadata` 前是否有授权与 key 前缀校验。

安全模式

- 所有敏感值使用 `kvs.set(key, value, { secret: true })` 与 `kvs.get(key, { secret: true })`。
- 部署期非敏感配置使用 `environment.variables`；密钥必须使用 KVS `secret: true` 加密存储。Nexus 平台不提供专用的密钥管理服务。
- Remote 场景使用 `auth.userToken` / `auth.appToken` 由平台自动注入令牌，避免自行持久化 PingCode Token。
- 在所有日志路径中对密钥进行脱敏（红action）。
- NOS key 使用用户/项目/租户前缀，并在生成预签名 URL 前做对象级授权。

CWE 参考

- CWE-798：使用硬编码凭据
- CWE-312：明文存储敏感信息
- CWE-532：将敏感信息写入日志文件
- CWE-522：凭据保护不足
